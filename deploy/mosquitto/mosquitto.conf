# Local-only broker: the controller (host) publishes and Home Assistant (host network)
# subscribes, both via 127.0.0.1:1883. Not exposed to the LAN.
listener 1883 127.0.0.1
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
